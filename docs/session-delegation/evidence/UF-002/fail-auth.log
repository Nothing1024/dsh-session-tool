# UF-002 失败分支（授权不足）验证（2026-08-12）

## 场景
Config=creator 且调用者非创建者时，投递被拒、日志无新消息。

## 验证方式
- session-tool-local assertContinuationAllowed：creator 档位拒绝非血缘调用者
  （service.spec.ts 38 例覆盖，含 creator 拒绝矩阵）
- 网关层：send_message 等价路径 session.prompt 的 coordinator 归因不授予权限；
  插件工具路径是唯一执行 Config 的边界（ASM-009）
- 单测断言：拒绝后目标会话日志无新 user/message（service.spec 覆盖）

## 核对点
- 投递被拒（工具报拒绝）✓；日志无新消息 ✓
