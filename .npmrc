# Belt-and-suspenders: copy instead of hardlink/clone from the global store.
# The actual brand-isolation fix is wiping packages/*/node_modules and
# reinstalling so symlinks stay inside this repo's .pnpm (not dsh-grok-bot).
package-import-method=copy
